# wds + esbuild Test App

## Setup

- `$ npm i`
- `$ npm start`

## Error

```shell
Error while transforming src/components/app-shell/app-shell.ts: Transforming JavaScript decorators to the configured target environment ("es2022") is not supported yet
```